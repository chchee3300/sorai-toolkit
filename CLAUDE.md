# Claude 自動化測試指引 (sorai-toolkit hub)

這是 SORAI Toolkit 的**主 repo**（Neutralino 殼層 + hub 選單 + packaging/CI/release）。目前狀態：多 repo 重構的 **Phase D 完成**（三個 repo 都到位：hub 本身、`sorai-toolkit-converter`、`sorai-toolkit-downloader`，兩個工具都透過 npm git dependency 消費；packaging/CI/release 全部搬到這個 repo）——完整分階段計畫在 `~/.claude/plans/mac-linux-reactive-metcalfe.md`。四個 Phase（A/B/C/D）都已完成；後續是計畫之外的新工作（語言選單/設定頁等 hamburger menu 剩下的項目、更多工具等）。

## 工作慣例
- **每次 commit（不管是這個 hub repo，還是 `sorai-toolkit-converter`/`sorai-toolkit-downloader` 那兩個 repo）完成後，都要同步更新這份 CLAUDE.md**，把這次改動實際影響到的行為/檔案/流程寫進對應章節（通常是「現況」），讓這份文件隨時反映目前程式碼的真實狀態——不要留給下一個 session 自己重新翻程式碼、重新踩一次同樣的坑才發現。小到「加了一個 badge」不必寫，但任何新增的檔案、新的執行期流程、或修正了某個既有假設，都要記。
- **本機開發一律用 `npm run dev:watch`，不要直接下 `neu run`**（`scripts/dev-watch.mjs`）。原因：Converter/Downloader 是透過 npm git dependency 裝進 `node_modules` 的（`prepare` script 建置一次），改這兩個 repo 自己 checkout 出來的原始碼並不會反映到 hub 的 `node_modules` 裡；而且實測過 `vite build --watch` 本身也不會理會 `node_modules` 裡的變化（rollup watcher 預設排除 `node_modules/**`）。`dev:watch` 做的事：先把兩個工具 repo 的 lib 建置一次、把 `dist/` 複製進 hub 的 `node_modules`、建置 hub 本身，然後啟動 `neu run`；之後只要存檔（不管改的是 hub 自己的 `src/`/`resources/`，還是旁邊 `sorai-toolkit-converter`/`sorai-toolkit-downloader` 兩個 sibling repo 的 `src/`），就會自動重跑同一條鏈，`neu run` 自帶的 `--neu-dev-auto-reload` 負責讓視窗跟著重新整理。預設假設兩個工具 repo 跟 hub 是同一層的 sibling 資料夾（`…/toolkit/sorai-toolkit`、`…/toolkit/sorai-toolkit-converter`、`…/toolkit/sorai-toolkit-downloader`），路徑不同可用 `CONVERTER_REPO`/`DOWNLOADER_REPO` 環境變數覆寫。`fs.watch({ recursive: true })` 只支援 Windows/macOS，Linux 上這支 script 目前不能用。

## 現況（Phase D）
- `src/App.jsx` 是 hub 殼層：`currentTool` state 切換 `HubMenu`/`ConverterApp`/`DownloaderApp`，沒有 router。兩個工具都從各自的 npm 套件 import（`sorai-toolkit-converter`、`sorai-toolkit-downloader`），不是本地檔案。
- Converter/Downloader 的所有元件/hooks 都不在這個 repo 裡——分別活在各自的 repo，透過 `node_modules/<pkg>/dist/index.js`（Vite library build）提供。`package.json` 的 `dependencies` 有兩個 `github:chchee3300/...` 條目，npm install 時會自動觸發各自的 `prepare` script 建置。
- `resources/js/lib/*.js`（`platform.js`、`ffmpeg-commands.js` 等）**仍然留在這裡**——這些是 `window.EstellaLib.*` 執行期全域變數，兩個工具的元件執行時都會呼叫它們，由 hub 統一提供這些 runtime globals。`platform.js` 已經有 `ytdlpPath()`（Phase D prep 加的），跟 `ffmpegPath()`/`qpdfCommand()`/`img2pdfCommand()` 同一套模式。
- `src/components/HamburgerMenu.jsx` 取代原本的 `#theme-toggle`：語言切換 + 深色/淺色切換 + 檢查更新 + 關於，設定頁之後再做。下拉面板用 `liquid-glass-react`，是玻璃面板的參考實作——做任何玻璃 UI 前先讀下面的「Liquid glass 製作指南」。
- **「回到主選單」已經不在 hamburger 選單裡了**——改成 `Header.jsx` 左上角 logo 圖示本身可以點擊（`showBackToHub` 為真時，`.logo-mark` 包一層 `<button className="logo-mark-button">`，`onClick={onBackToHub}`；在 hub 首頁本身則是純 `<img>`，不可點擊）。**只有圖示本身是點擊區，「Toolkit」文字跟麵包屑一律維持純 `<span>`**——文字讀起來像標籤不像按鈕，刻意不把它們納入點擊區。Hover 回饋是圖示本身 `transform: scale(1.12)`（`.logo-mark-button:hover .logo-mark`），不是文字變色——文字變色曾經是第一版做法，使用者回饋「樣式不喜歡」後改掉。圖示按鈕的 `aria-label` 用新的 `header.backToHub` 字典 key（`src/i18n/dict.js`），因為 `<img alt="">` 本身沒有可讀名稱。`hamburger.mainMenu` 字典 key 跟對應的 `HomeIcon`/`.hamburger-item--danger` CSS 規則都已經一併刪除，不要再假設它們存在。
- Header 的麵包屑現在顯示**工具名稱**（如「Converter」/「Downloader」），不是舊版動態的檔案類型徽章。版本號顯示也搬到這裡的 `Header.jsx`（`.header-version`，讀 hub 自己的 `src/version.json`）——兩個工具套件都沒有自己的版本號顯示，避免組合後顯示錯誤版本。
- **自動更新現在區分「官方 CI 打包」跟「自架/自己 build」兩種情境，唯一的判斷依據是 `useUpdateChecker.js` 的 `IS_OFFICIAL_BUILD`**（`import.meta.env.VITE_OFFICIAL_BUILD === 'true'`）——這個環境變數只有 `.github/workflows/release.yml` 的三個 build job 在 `npm run build` 那一步會設,本機/自架的 `npm run build` 從來不會設,不需要任何額外設定就能自動落到「非官方」那條路。兩種情境檢查 GitHub Releases 的邏輯完全一樣（`checkForUpdate`,readonly,兩邊都會跑）,差在 `installUpdate`：official build 維持原本「下載安裝檔 + Windows 靜默安裝 + 自動重開」那套；非 official 直接 `Neutralino.os.open(latestRelease.html_url)` 開瀏覽器到 GitHub release 頁面,不下載也不裝,因為自架版本不保證跟官方 release 產物一致,沒有身分可信的替換基礎。UI 呈現也跟著分流：`App.jsx` 只有 `updater.isOfficialBuild` 為真才會掛載 `UpdateBanner`（右下角 toast）——非官方 build 完全不會跳出這個 toast,唯一的更新提示管道是 `HamburgerMenu.jsx` 漢堡按鈕本身 + 「關於」那一列的一個小圓點徽章（`.hb-badge-dot`,`updater.status === 'available'` 時顯示）,點進「關於」之後 `AboutModal.jsx` 會多一段 `.about-modal-update` 區塊,按鈕文字依 `isOfficialBuild` 顯示「立即更新」或「在 GitHub 上查看」,但兩邊按鈕都直接呼叫同一個 `updater.installUpdate`——它自己內部已經分流,元件不需要知道細節。這整套已經在真實 WebView2 視窗裡用 Playwright route 攔截 `api.github.com` 偽造一個更新版本驗證過（本機 dev build 沒設那個環境變數,天然就是「非官方」路徑）：確認沒有 toast、兩個徽章圓點都有出現、「關於」內的更新區塊文字/按鈕正確、點擊後真的呼叫 `Neutralino.os.open` 而不是下載安裝。
- `useTheme.js`/`useUpdateChecker.js`/`UpdateBanner.jsx` 都在這裡（hub 層級關注點）。`localStorage` key 是 `sorai-theme`。`useUpdateChecker.js` 的 `REPO` 常數指向 `chchee3300/sorai-toolkit`。
- **字型現在是本地 bundle，不是全部走 Google Fonts CDN**：`resources/fonts/` 放了 Inter（400/500/600，latin + latin-ext）跟 Noto Sans TC／思源黑體（400/500/600，chinese-traditional 子集，~1MB/檔）的 woff2，`resources/styles.css` 開頭用 `@font-face` 本地載入這兩個字重，取代原本的 `@import googleapis`；JetBrains Mono 還是走 CDN（沒動它）。每個 `font-family` stack（不管是 Inter 那套還是 JetBrains Mono 那套）都加了 `'Noto Sans TC'` 當 CJK fallback，所以中文（含檔名裡的中文字）在 mono 排版的地方也不會 fallback 回系統預設字體。改動這兩個字型檔要記得 `.queue-item-title` 之類原本假設 `<p>` 的 class 現在也套在 `<span>` 上（Converter 的 `FileList.jsx` 標題保留 `<span title=...>` 是為了不動到既有測試選擇器），所以 CSS 有補一條 `display: block`。
- **`binaries/<platform>/`** 現在同時有 ffmpeg（Converter 用）跟 yt-dlp（Downloader 用，透過 `--ffmpeg-location` 共用同一份 ffmpeg，合併分離的 video/audio 串流）。`setup.mjs` 的 `setupYtDlp()` 從 yt-dlp 自己的 GitHub releases API 動態抓最新版（不是釘死版本），macOS 兩個 arch 資料夾共用一份 universal build（跟 ffmpeg 的 evermeet.cx build 同一套邏輯，CI 的 macOS job 也有對應的「populate other arch」複製步驟）。
- **`sorai-toolkit-converter` repo**（本機路徑實際是 `E:\資料\SORAI 專案\toolkit\sorai-toolkit-converter`，跟 hub 同一層 sibling 資料夾——不是舊的 `E:\資料\tools\FileConverterApp`，那個路徑已經不存在了）：`vite.lib.config.mjs` 產生 `dist/index.js`；`src/index.js` barrel export `{ ConverterApp }`。它自己的 `packaging/`/`.github/workflows/release.yml`/`.releaserc.json` 已經在 Phase C 完成後**退休移除**了——不會跟 hub 的版本衝突。`FileList.jsx` 現在跟 Downloader 的 `QueueList.jsx` 一樣，每個檔案都顯示縮圖 + 來源資訊卡片（見下面縮圖那條）。
- **`sorai-toolkit-downloader` repo**（本機路徑實際是 `E:\資料\SORAI 專案\toolkit\sorai-toolkit-downloader`，同樣是 hub 的 sibling 資料夾，不是舊的 `E:\資料\tools\sorai-toolkit-downloader`）：跟 Converter 同一套 dual-purpose 形狀（standalone dev harness + library build），`src/index.js` barrel export `{ DownloaderApp }`。核心邏輯在 `src/lib/ytdlp.js`（yt-dlp 指令建構 + `--progress-template` 輸出解析）+ `src/hooks/useDownloader.js`（`execCommand` 抓 metadata、`spawnProcess` + `spawnedProcess` 事件跑下載，跟 Converter 的 `useExecute.js` 同一套模式）。**選到 video-only 格式時，實際丟給 yt-dlp 的 `-f` 值會自動變成 `<format_id>+bestaudio`**（`resolveFormatSelector`）——單獨選 video-only 格式不會自動觸發合併，這是已經修過、有測試覆蓋的一個坑，不要漏掉。
- **Converter 的檔案列表縮圖預覽**：`FileList.jsx` 的每一行現在共用 hub `styles.css` 的 `.queue-item*` class（跟 Downloader 的 `QueueList.jsx` 同一套視覺語言），video/image/audio 三種類型會顯示縮圖（PDF 維持原本精簡列表，不受影響）；縮圖由 hub 的 `ffmpeg-commands.js` 新增的 `buildThumbnailCommand` 產生（video 取中間影格、audio 取內嵌封面圖、image 直接縮小），寫進一個 per-session 暫存資料夾，透過 `Neutralino.server.mount('/thumbs', ...)`（跟 `TrimModal.jsx` 的 `/preview` mount 同一套手法，只是活得比較久、指向產生出來的檔案而不是來源目錄）用 `localhost` URL 提供給 `<img>`。取不到縮圖（多半是沒有內嵌封面的音訊檔）會 fallback 成空的 placeholder box，不是報錯。這幾行邏輯都在 `useFileManager.js`（`getThumbnail`/`ensureThumbDir`）。同一批改動也把來源影片的原始編碼（`h264`/`hevc`/`vp9`/`av1`…，從 `ffmpeg -i` 的 `Video: ` 那行解析出來）當成一個 badge 顯示在 fps 旁邊。
- **App icon 換成 STUDIO SORAI BRANDKIT 的新 logo，而且是主題感知的**：`resources/icons/appIcon-dark.png`/`appIcon-light.png`（1024x1024，window/taskbar icon 用）+ `appIcon-dark.ico`/`appIcon-light.ico`（256x256，用 hub 自己的 ffmpeg 轉檔，跟 Converter 的 image-to-ico 指令同一套）都是從原始 2835x2836 的品牌 PNG 轉出來的；`appIcon.png`（`neutralino.config.json`/packaging 腳本引用的原始檔名）是 dark 版本的複製，維持路徑不變。`useTheme.js` 在切換主題時會呼叫 `Neutralino.window.setIcon()` 即時換掉視窗/工作列圖示（白色搭深色主題、黑色搭淺色主題）——這需要 `neutralino.config.json` 的 `nativeAllowList` 加上 `"window.*"`，沒加會靜默失敗。`Header.jsx`、`AboutModal.jsx` 兩處都各自根據 `theme` prop 挑對應版本，兩處都要記得改，忘記其中一處會出現「logo 沒跟著主題換」的問題（踩過一次）。Header 的文字現在只剩「Toolkit」，不寫「SORAI」了（logo mark 本身就代表 SORAI）。
- **HamburgerMenu 多了「關於」項目，`AboutModal.jsx` 不是玻璃面板**：顯示版本/開發者/授權/專案首頁/簡介 + 每個 bundled 第三方工具的授權連結（對照 `THIRD-PARTY-LICENSES.md`）。這個元件一度是這份文件「Liquid glass 製作指南」的第二個玻璃面板範例，後來因為 backdrop-filter 在這個引擎對這個殼層完全不繪製、而且拿掉 backdrop-filter 之後 `liquid-glass-react` 本身還是會在文字外圈留一圈陰影暈染（見下面指南第 7 條的完整 debug 記錄），最後把整個 `<LiquidGlass>` 拿掉，改成跟 Trim/Crop/MixedType 一樣的純 `.modal-content`（`.about-modal-content` 只覆寫 `max-width`）。**以後新增這種內容密集的面板（設定頁等），直接照 Trim/Crop 的 `.modal-content` 模式做，不要套用玻璃語言**——玻璃語言是 hamburger 下拉選單那種短標籤選單的專屬語法。透過 `createPortal` 掛到 `document.body`（`HamburgerMenu.jsx` 本身是 `position:relative`，會把全螢幕 overlay 的定位錨點限制在按鈕自己的小方塊裡）。

- **背景執行（系統匣）+ 設定頁**：點視窗右上角關閉鈕不再直接結束程式——`src/hooks/useCloseBehavior.js` 統一接管 `windowClose` 事件（`src/main.jsx` 不再自己註冊這個監聽器），依 `localStorage` 的 `sorai-close-behavior`（值 `ask`/`tray`/`quit`，預設 `ask`）分流：`ask` 開 `CloseConfirmModal.jsx`（跟 `AboutModal.jsx` 同款純 `.modal-content` 殼，非玻璃，有「記住我的選擇」checkbox）；`tray` 直接呼叫 `Neutralino.os.setTray()`（沿用原本沒人用的 `resources/icons/trayIcon.png`）+ `window.hide()`，第一次縮到背景會跳一次 `showNotification`；`quit` 直接 `app.exit()`。系統匣選單有「開啟」跟「結束程式」兩項，點「開啟」呼叫 `window.show()+focus()`。新增的「設定」頁（`src/components/SettingsModal.jsx`，`HamburgerMenu.jsx` 新增一個入口，跟「關於」同款）目前只有一個 section：關閉行為三選一 radio，之後要加新設定就照這個「一組 `.settings-section`」的形狀擴充，不用重構。`useUpdateChecker.js` 的靜默安裝重開流程那個 `app.exit()` 呼叫維持原樣，不經過這個新的 confirm 流程。跨平台已知限制：macOS 沒有 API 可以連 Dock icon 一起藏起來；Linux 系統匣要不要顯示看桌面環境（GNOME 預設要裝擴充套件才有系統匣）——只在 Windows 上真機測過。
- **Converter 新增「快速壓縮到目標大小以下」**：`SettingsPanel.jsx` 的 `VideoSettings` 新增 5/25/50MB 三個 preset chip（`useSettings.js` 新增 `TARGET_SIZE_PRESETS_MB`/`video.targetSizeMode`/`video.targetSizeMB`），開啟後畫質滑桿隱藏、GIF 格式選項排除、codec 下拉選單強制鎖定 `libx264`（不只是 UI 顯示層面的鎖定——`useExecute.js` 呼叫 `buildVideoCommand` 時無論 `settings.video.codec` 實際存什麼都強制覆寫成 `libx264`，因為 `-maxrate`/`bufsize` 的 VBV 強制力在另外三個 codec 上沒那麼可靠）。位元率算法在 hub 的 `resources/js/lib/ffmpeg-commands.js` 新增 `computeTargetSizeBitrates`（十進位 MB 不是 MiB、8% 安全邊際 `TARGET_SIZE_SAFETY_MARGIN`、音訊碼率依總預算動態 clamp 在 32-128kbps 之間，避免小目標被固定 128k 音訊吃光預算），單一 pass（`-b:v`/`-maxrate`/`-bufsize`），不做兩階段編碼——需求是「低於門檻」不是「精準命中」。真機測過（2 秒測試片段）：輸出確實低於目標、`h264`/`aac` codec 正確套用、零錯誤；長片/高動態影片的安全邊際校準還沒做過，這個常數如果之後發現常常超標要回頭調。`estimateDisplay.js` 在 target-size 模式下直接顯示 `targetSizeMB * 0.95`，不跑跟這個模式無關的相對品質公式。
- **Windows 檔案總管右鍵選單整合（macOS/Linux 還沒做，見下方獨立條目）**：右鍵一個影片/圖片/音訊/PDF 檔案會有一個「SORAI Toolkit」層疊選單（`packaging/windows/generate-context-menu-registry.mjs` 在 `build.ps1` 跑 `iscc` 之前產生 `packaging/windows/context-menu.generated.iss`，gitignore、每次建置重新產生，`installer.iss` 用 `#include` 引入；格式清單對照 Converter 的 `useFileManager.js`（輸入判定，範圍比輸出格式寬）跟 `useSettings.js`（輸出格式），手動保持同步、沒有自動檢查，Converter 改格式清單要記得同步這支腳本）。點擊會用 `--sorai-convert-to=.ogg`/`--sorai-quick-action=compress-under:50`/`compress-pdf` 這幾種命令列參數啟動（或喚醒已開啟的）app。新增的 `src/lib/launchArgs.js` 解析 `window.NL_ARGS`（已確認 Neutralino 真的會把額外附加的命令列參數原封不動放進 `NL_ARGS`，不會過濾掉不認識的 flag）；`src/lib/instanceLock.js` + `src/hooks/useSingleInstance.js` 處理單一實例偵測（`<Neutralino.os.getPath('data')>/sorai-toolkit/instance.lock` 存 PID，`isPidAlive` 依平台跑 `tasklist`/`kill -0` 判斷；有 jitter + 寫入後回讀驗證但不是真正的原子鎖，最壞情況是兩個視窗同時開，不會遺失檔案——reload-safe，lock 裡的 PID 是自己的話直接視為 primary，不會誤判成「已經有一個自己在跑」而自我結束，這個坑在 `--neu-dev-auto-reload`/手動 reload 情境下會直接把 dev session 炸掉，修過了）。`App.jsx` 用 `pendingLaunch`（`{seq, files, format, quickAction}`，`seq` 遞增讓「已開啟時又被轉發」跟「冷啟動帶參數」共用同一套消費邏輯）prop 把解析結果傳進 `ConverterApp`，`ConverterApp` 的 `handleFirstFileType` 在 `useFileManager` 既有的「用來源副檔名當預設格式」掛勾點上覆寫格式/切換 target-size 模式。全流程（真的登錄檔項目、真的 app exe、真的滑鼠右鍵點擊）在真機上驗證過：右鍵 → 轉檔 → 轉成 X → app 開啟、檔案進佇列、格式正確套用。
- **`MultiSelectModel=Player` 在真機上不會把多選檔案一次送進同一次命令列呼叫**——這是原始計畫的錯誤假設，已經在真機（真的 Windows 檔案總管 + 真的登錄檔）驗證推翻：Windows 靜態右鍵動詞的預設模型（`Single`，不設 `MultiSelectModel`）才是正確答案，多選 N 個檔案會觸發 N 次個別呼叫，每次命令列裡的 `%1` 只有一個檔案路徑；`Player` 模型是給媒體播放器「重用已開啟的實例」用的語意，不是「多路徑塞進一次呼叫」，設了之後命令直接靜默不執行（真機測過：右鍵選單正常顯示、點了完全沒反應，連測試用的 log 檔都不會產生）。所以 `pending-launch` 機制是目錄＋去抖動，不是單一檔案覆寫——每個 Explorer 呼叫各自把自己的單一檔案寫進 `<state dir>/pending-launch/` 目錄裡一個獨立檔名（`instanceLock.js` 的 `enqueuePendingLaunch`），primary instance（`useSingleInstance.js`）每 300ms 輪詢一次目錄列表，偵測到列表 500ms 內沒有變化才視為「一次多選點擊的所有檔案都到齊了」，一次性讀取、合併、清空、觸發一次 `pendingLaunch`（`POLL_INTERVAL_MS`/`DEBOUNCE_MS` 常數）。這個坑如果 macOS（Automator Quick Action）、Linux（Dolphin ServiceMenu／Nautilus Scripts）要做，多選行為要重新假設、重新做各自的最小 spike 驗證，不能直接照抄 Windows 這套「一次全部進來」的期待去設計。
- **`neutralino.config.json` 的 `modes.window.hidden` 改成 `true`**（原本 `false`）——全域行為改動，不只影響右鍵啟動路徑：現在每次啟動視窗預設是隱藏的，由 `useSingleInstance.js` 判定自己是 primary instance 之後才主動呼叫一次 `Neutralino.window.show()`；secondary instance（右鍵多選觸發的第 2/3/... 個呼叫，或已有視窗開著時再次右鍵）從頭到尾不呼叫 `show()`，寫完 pending-launch 檔案、嘗試把既有視窗拉到前景之後就直接 `app.exit()`，不會有視窗閃現。這個改動連正常雙擊啟動、靜默自動更新重開（`useUpdateChecker.js`）都會經過同一條路徑，已經一起測過沒有問題。喚醒已開啟視窗的機制（`instanceLock.js` 的 `bringExistingInstanceToForeground`）依平台分流：Windows 用 `os.execCommand` 跑一段 base64 `-EncodedCommand` 傳遞的 PowerShell（`EnumWindows`+`GetWindowThreadProcessId`+`SetForegroundWindow`，不是 `Get-Process.MainWindowHandle`，那個對縮到系統匣後隱藏的視窗不可靠）——這條真機測過會動；macOS 預計用 `osascript activate`、Linux 用盡力而為的 `wmctrl -a`，都還沒機會在真機驗證。失敗一律吞掉，不影響檔案有沒有正確送進 primary instance 的佇列，喚醒前景只是錦上添花。
- **Feature B 的 macOS（Automator Quick Action 自我安裝到 `~/Library/Services/`）跟 Linux（Dolphin ServiceMenu 系統層級安裝 + GNOME Nautilus Scripts 自我安裝）還沒實作**——Windows 這邊做完才發現原計畫對 `MultiSelectModel` 的假設是錯的（見上一條），這兩個平台開工前要重新假設多選行為、重新做各自的最小 spike，不能直接照抄 Windows 已經驗證過的設計搬過去。完整計畫記錄在 `~/.claude/plans/synthetic-snacking-zephyr.md`。
- **Converter 的檔案佇列面板高度沒吃滿可用空間的 bug**（真機測試時使用者截圖抓到的，跟上面幾條 Explorer 整合功能無關，是順手修的）：`FileList.jsx` 的容器 class `.file-list` 從縮圖卡片改版後一直沒跟著調整，還留著改版前給單行 `.file-item` 用的 `max-height: 148px`，導致檔案少的時候面板下面留一大塊黑色空白、檔案多的時候卡在 148px 高度不會照 `.panel--ghost` 實際可用空間伸展。改成跟 Downloader 的 `.queue-list` 同一套 `flex:1; min-height:0; overflow-y:auto`，另外補上 `#file-list-container` 本身也要 `flex:1`（原本完全沒有這條規則，flex column 裡一個沒宣告 `flex:1` 的 item 預設不會伸展）。hub 跟 Converter repo 各自的 `resources/styles.css` 都要一起改，這兩份 CSS 本來就是手動保持同步的兩份複本，不是同一個檔案連結。

## Packaging/CI/Release（Phase C 搬進來的，全部以這個 repo 為準）
- `packaging/{linux,windows,macos}/`、`.github/workflows/release.yml`、`.releaserc.json`、`scripts/{get-next-version,write-version}.mjs` 都是從舊 FileConverterApp repo 搬過來的，搬的時候做了**耦合修正**（這幾個一定要一起改，改一半會讓打包 job 靜默失敗）：所有 packaging 腳本裡 `dist/FileConverterApp/FileConverterApp-<platform>` → `dist/sorai-toolkit/sorai-toolkit-<platform>`（因為這裡的 `neutralino.config.json` 的 `cli.binaryName` 已經是 `"sorai-toolkit"`，`neu build` 的輸出路徑跟著變了）；`packaging/macos/Info.plist` 的 `CFBundleIdentifier` → `com.soraitoolkit.hub`；`packaging/linux/build.sh`/`installer.iss` 的 repo URL → 這個 hub repo 網址。
- **已經用真的指令驗證過，不是只看過程式碼**：`npm run build` → `neu build --release --embed-resources`（確認輸出真的在 `dist/sorai-toolkit/sorai-toolkit-win_x64.exe`）→ `pwsh packaging/windows/build.ps1 -Version <ver>`（真的呼叫本機已裝好的 Inno Setup 7 編譯出 `release-assets/sorai-toolkit-setup-<ver>-win_x64.exe`）→ 用 `/VERYSILENT` 實際安裝到 `%LocalAppData%\Programs\SORAI Toolkit\`（`PrivilegesRequired=lowest` 決定裝在使用者層級而非系統層級）→ 確認 `[Run]` postinstall 真的自動重開 app（沒有 `skipifsilent`）→ 截圖確認原生視窗標題是「SORAI Toolkit」、hub 選單正確顯示、版本號 `v0.1.0`、Converter 卡片存在、log 檔沒有 404 類錯誤（只有預期內、無害的 `favicon.ico` 404）→ 測完用 `/VERYSILENT` 跑 `unins000.exe` 解除安裝、清掉殘留資料夾，機器狀態還原乾淨。
- hub 的 `package.json` 新增了 `@semantic-release/*` 系列 devDependencies + `"release": "semantic-release"` script（跟舊 Converter repo 當初的版本號對齊）。
- **`packaging/windows/generate-context-menu-registry.mjs`**：Windows 檔案總管右鍵選單的 `[Registry]` 產生腳本（見上面現況段落的完整說明），`build.ps1` 在跑 `iscc` 之前會先跑這支。輸出的 `packaging/windows/context-menu.generated.iss` 是 gitignore 的建置產物，不進版控。

## 繼承自舊 FileConverterApp repo 的關鍵注意事項（都還適用）
- **`neutralino.config.json` 的 `cli.resourcesPath` 必須等於 `documentRoot`**（現在都是 `/web-dist/`）——不一致會讓打包版本開啟後變 404，`neu run` 測不出來，必須真的 `neu build --release` 測。
- **`defaultMode` 必須是 `"window"`**，不是 `"browser"`。
- **`modes.window.enableInspector` 必須是 `false`**。
- **`src/main.jsx` 必須監聽 `windowClose` 並呼叫 `Neutralino.app.exit()`**（`exitProcessOnClose: false` 需要這個才能讓關閉鈕生效）。
- **`useUpdateChecker.js` 下載 release asset 不能用瀏覽器 `fetch()`**（CORS 會擋掉），要用 `Neutralino.os.execCommand` 呼叫 `curl`。
- **`@neutralinojs/neu` 鎖在 `11.7.1`**（`latest` 的 `uuid` 依賴是 ESM-only，會讓 `npm install -g` 直接崩潰）。
- **解壓縮 zip 不能假設裸指令 `tar` 支援**（Windows 用 `System32\tar.exe` 全路徑，macOS/Linux 用 `unzip`）。
- **Linux `.deb`/`.rpm` 宣告 `gtk3`/`webkit2gtk` 依賴**（deb: `libgtk-3-0`/`libwebkit2gtk-4.1-0`；rpm: `gtk3`/`webkit2gtk4.1`）——Neutralino 開視窗需要，別拿掉。
- **`playwright` 必須是 `devDependencies`**，且 CI 全域設 `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: '1'`——沒有的話每個 job 的 `npm ci` 都會下載瀏覽器，曾在 macOS runner 上因此炸硬碟。
- **`installer.iss` 的 `[Run]` 項目不能有 `skipifsilent`**——應用內更新用 `/VERYSILENT` 跑安裝檔，`skipifsilent` 會讓「安裝完自動開啟」完全不執行。

## Liquid glass 製作指南（app 內所有玻璃面板都必須 follow）
目前的參考實作是 hamburger 下拉選單（`src/components/HamburgerMenu.jsx` + `resources/styles.css` 的 `.hamburger-dropdown` 區塊）。之後任何新的玻璃面板（設定頁、新 dropdown、popover 等）都照這套規則做：

1. **玻璃語言（使用者實機 A/B 過好幾輪才定案的，不要自作主張改）**：`backdrop-filter: blur(1px) saturate(1.6)` + 淡 tint（深色 `rgba(13,16,22,0.3)`、淺色 `rgba(242,243,247,0.6)`，淺色必須跟 `--bg` 同色系）。重點是「近乎透明、後面內容看得到」——重霧面（如 blur 24px）不管 tint 調多少都像一塊實心板，一律不用。
2. **backdrop-filter 一律放在面板最上層的殼層元素**。這個 WebView2 引擎裡，backdrop-filter 放在任何後代元素上都是「computed 正確但完全不繪製」（實機 bisect 驗證過，`.glass-lens--flat` 跟 `.glass__warp` 都中過同一族 bug）。玻璃的 tint/blur 全部自己在殼層做，永遠不要依賴 library 內部的層。
3. **用 liquid-glass-react 時必須加的三個 CSS 修正**（照抄 `.hamburger-dropdown` 的做法）：
   - `.glass__warp` → `display:none`：它畫不出霧面，但它的 `filter: url(#...)` SVG 濾鏡鏈會合成一層均勻暗紗（bisect 實測：藏掉它面板從 (229,231,236) 回到跟背景一致的 (242,243,247)）。
   - `> .bg-black` → `display:none`：Tailwind v4 自動內容偵測不掃 node_modules，library 的 `opacity-0/20/100`、`mix-blend-overlay` gating class 都不存在、`.bg-black` 卻碰巧存在——兩個黑色 wash div 永遠全不透明（`overLight` prop 形同虛設；深色模式黑疊黑看不出來，淺色模式直接一塊黑）。`overLight` 固定傳 `false`。
   - library 行內的 `transition: all 0.2s` 全部 `transition: none !important` 關掉——不關的話，開啟動畫播放中途的重新量測會疊一段慢速尺寸過渡，變成黏糊感。
4. **開關動畫必須跟 `.lg-dropdown` 完全同參數**：同 keyframes（`lg-pop-down`/`lg-pop-up-out`）、0.14s/0.1s、置中 transform-origin。進出都用 ease-out，永遠不用 ease-in。動畫要 gate 在尺寸量測完成之後才開始（不然會在 `visibility:hidden` 期間播完、看起來像直接閃現）；動畫結束後要把 class 拆掉——forwards-fill 會讓殼層永久 GPU-promoted，而 promoted 元素的後代 backdrop-filter 在這個引擎不繪製。
5. **面板顏色不對（變灰/變暗）時，先實機逐層 bisect 找元兇，不要反射性調 tint alpha**——歷史上的灰就是 `.glass__warp` 暗紗造成的，跟 alpha 無關。
6. **驗證一律在真實 WebView2 視窗做**：`neu run` + `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9229` + Playwright `connectOverCDP`，截圖後用 System.Drawing 取樣像素比對（模板參考 git history 裡的 `tests/_verify_*.js`，用完即刪）。瀏覽器裡測不出這個引擎的 backdrop-filter 行為。
7. **面板疊在「內容密集」的背景上時（不是 hamburger 那種短標籤選單），backdrop-filter 不可靠，不要預設它會生效**——`AboutModal.jsx` 曾經是第二個玻璃面板，疊在 hub-grid 卡片上時 `blur(1px)`（掃到 `blur(40px)` 都一樣）完全擋不住背景文字透出來，bisect 實機（含正牌 WebView2 視窗，`neu run` + `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9229` + Playwright `connectOverCDP`，不是另開一個瀏覽器分頁測，那樣測不出這條）確認 backdrop-filter 在這個殼層上根本沒有繪製，不是強度不夠；`.hamburger-dropdown` 自己的 blur 有沒有真的在畫也沒驗證過，它從來沒疊在夠「忙」的背景上讓這問題露餡，可能是同一個坑只是沒人發現。**光拿掉 backdrop-filter 還不夠**——面板改成完全不透明背景後，文字外圈還是留了一圈類似陰影的暈染邊緣（使用者實機截圖回報），代表 `liquid-glass-react` 本身（不只 backdrop-filter 這一個屬性）還在用某種方式影響這個殼層的繪製。**最後的做法是把 `<LiquidGlass>` 整個拿掉，不是只拿掉 backdrop-filter**——`AboutModal.jsx` 現在跟 Trim/Crop/MixedType 一樣，是純 `.modal-content`（`var(--surface-1)` 實色背景、共用的 `.modal-overlay` scrim+`blur(8px)`、共用的 pop-in transition），不再是玻璃面板，也不再算這份指南的第二個範例——一旦面板不需要任何真的透明/模糊/折射效果，直接不要用這個 library，比繼續在它的內部機制裡找兇手划算。**這條的教訓**：內容密集的面板一開始就不該套玻璃語言（那是 hamburger 這種短標籤選單的專屬語言，短標籤背景沒什麼好讀的內容，低 alpha/沒模糊都無所謂）；真的要做「設定頁」這類密集內容的新面板，直接照 Trim/Crop 的 `.modal-content` 模式做，不要套用第 1-6 條的玻璃規則。

## 從程式碼註解挖出來、還算數的已知小坑
- **Playwright 測 React controlled input 不能用 `el.value = x` + dispatch('input')`**——React 覆寫了原生 `value` setter，這招在舊版 vanilla app 上有效，對現在的 React build 會靜默 no-op。要繞過原生 property setter：`Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set.call(el, x)`，再 dispatch `input`/`change`。`GlassSelect` 包出來的下拉不用這招，直接 `el.value = x` + dispatch 就有效。
- **`<select class="input">` 都被 `GlassSelect.jsx`（liquid-glass-react 包出來的自訂下拉）包住，原生 `<select>` 被設成 `display:none`**——Playwright 的 `selectOption()`/`.fill()` 對它會 hang 或丟錯，一樣要直接設 `.value` 再 dispatch `change`+`input`。
- **`.player-overlay`（TrimModal/ClipModal 的播放器控制列）預設 `opacity:0`，只有 `:hover` 才顯示**——刻意的 CSS，不是 bug；沒模擬 hover 就截圖會看到空的 modal，不要因此誤判成壞掉。
- **`useDownloader.js`/`useExecute.js` 的 `awaitWithCancelGuard`**：強制 kill 一個 spawned process（`Neutralino.os.updateSpawnedProcess(pid,'exit')`）不保證每個平台都會準時（或根本）觸發 `spawnedProcess` 的 `exit` 事件——沒有這個逾時保險，取消下載/轉檔可能永遠卡在「Cancelling...」。
- **語言自動偵測（`resources/js/lib/i18n.js`）只認 Traditional Chinese**（`zh-TW`/`zh-HK`/`zh-MO`/`zh-Hant-*`）**，Simplified Chinese（`zh-CN`/`zh-Hans`）會 fallback 回英文**——`SUPPORTED` 目前沒有簡體變體，是刻意範圍，不是偵測漏洞。
- **PowerShell 工具呼叫傳中文字串容易亂碼，且 `reg query`/`Get-ItemProperty` 在 Bash 終端機裡顯示出來的亂碼不代表登錄檔實際存的資料是壞的**——開發 Windows 右鍵選單整合時踩了兩層坑：(1) 透過 `Set-ItemProperty` 這類 PowerShell 指令直接傳入含中文的字串參數，寫進登錄檔的資料本身就可能真的壞掉（可靠寫法：在 Bash 裡用 Node.js 產生正確 UTF-16LE 編碼的 `.reg` 檔、`reg import` 匯入，而不是讓中文字串直接經過 PowerShell 工具呼叫）；(2) 就算資料寫得正確，`reg query` 這類原生工具的主控台輸出用系統 ANSI codepage（例如繁中 Windows 的 cp950），Bash/git-bash 終端機顯示出來一樣是亂碼，但這只是顯示層問題——要驗證登錄檔實際存的值，用 PowerShell 把值寫成 UTF-8 檔案（`[System.IO.File]::WriteAllText(path, $val, [System.Text.Encoding]::UTF8)`）再讀那個檔案，才是可靠的驗證方式，不要看 `reg query`/`Get-ItemProperty` 直接印在終端機上的文字。
- **`sorai-toolkit-converter/design-system/MASTER.md`**：vanilla→React 遷移的完整歷史記錄（Phase 0-3，2026-07-14 定案），活在 **Converter repo**，不是這裡——hub 的 `resources/js/lib/*.js` 檔案開頭註解裡「見 design-system/MASTER.md」都是指那份文件（這些 lib 檔案原本就是從舊 FileConverterApp/Converter 搬過來的）。內容屬於歷史記錄，已被這份 CLAUDE.md 的 Phase A-D 敘事取代，除非要追溯遷移當時某個決策的原始理由，否則不用重看——**裡面部分內容已經過時**（例如曾記錄 TrimModal/ClipModal 有個 `.thumb-grip` 點擊誤判 bug「未修」，但現在兩邊程式碼都已經是 `.contains()` 判斷，早就修好了），引用前先對照現在的程式碼確認還算數。

## 測試
- **Converter**：`node tests/test_conversion.js`、`test_drop.js`、`test_crop_ui.js`、`test_image_crop_commands.js`（每個瀏覽器導向的測試在 `page.goto` 後多了一步「先點 `.hub-card` 進入 Converter」，因為現在 hub-grid 有兩張卡片，Converter 卡片的點擊選擇器要小心不要跟 Downloader 的搞混）。改 Converter 邏輯要在 `sorai-toolkit-converter` repo 那邊改；本機開發用 `npm run dev:watch` 會自動抓到新版（見上面「工作慣例」），但這裡跑的 test script 是直接對著 hub 現有的 `node_modules`/`web-dist`，所以測之前要確定已經跑過一次建置（`dev:watch` 或手動 `build:lib` + 複製 + `npm run build`）；等 Converter repo 那邊的改動真的 commit + push 過，這裡才需要重新 `npm install` 抓正式的 git dependency 版本。
- **Downloader**：`node tests/test_download.js`——用 `.hub-card:has-text("Downloader")` 選到正確的卡片，實際貼一個真實短片網址（"Me at the zoo"，YouTube 上第一支影片，~19 秒）測試 metadata fetch + 真的下載，並且**特意選一個 video-only 格式**（不是預設值）確認 `--ffmpeg-location` 合併真的有效——用 `ffmpeg -i` 檢查輸出檔案同時有 Video 跟 Audio 串流，不是只確認檔案存在。原生資料夾選擇對話框沒辦法用 Playwright 驅動，測試裡用 `window.Neutralino.os.showFolderDialog = async () => dir` 直接 mock 掉。改 Downloader 邏輯要在 `sorai-toolkit-downloader` repo 那邊改；一樣本機開發用 `npm run dev:watch` 自動抓新版，commit + push 過後才需要重新 `npm install` 抓正式版本。
- 全部一起驗證過（Phase D 完成時）：`rm -rf node_modules package-lock.json && npm install` 觸發兩個套件的 `prepare` build，`npm run build` 成功，5 個測試套件全部通過（60+22+13+19+11 = 125 個檢查）。
